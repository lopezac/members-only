doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/modern-normalize.css')
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    header(class="topHeader")
      nav
        ul
          li #[a(href="/") Home]
          li #[a(href="/users") Users]
        ul(class="right-ul")
          if (currentUser)
            li #[a(href="/message/create") Create a new message]
            if (!currentUser.membership)
              li #[a(href="/join-club") Join the Club]
            li
              form(action="/sign-out" method="POST")
                button(type="submit" class="btn-sign") Sign Out
            li #[a(href=currentUser.url)= currentUser.username]
          else
            li #[a(href="/sign-up" class="btn-sign") Sign Up]
            li #[a(href="/sign-in" class="btn-sign") Sign In]
    main
      div(class="container")
        block content
    footer
      p Axel C. Lopez @Copyright 2022 
        a(href="https://github.com/lopezac") GitHub
          //- img(class="icon" src="/icons/github.png" alt="GitHub")